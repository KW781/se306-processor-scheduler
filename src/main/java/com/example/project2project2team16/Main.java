package com.example.project2project2team16;

public class Main {
    /**
     * This method only exists as an entry point to the application for when running the application as a JAR file from
     * the terminal - it is not the only entry point, and in IntelliJ you can set up a different run configuration so
     * that HelloApplication.main() is run as the entry point instead.
     * @param args List of command line arguments when JAR is run from the terminal
     */
    public static void main(String[] args) {
        HelloApplication.main(args);
    }
}
